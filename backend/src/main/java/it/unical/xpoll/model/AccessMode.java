package it.unical.xpoll.model;

/**
 * Enum representing the access mode for a user.
 * LOCAL: User registered with email/password
 * GOOGLE: User authenticated via Google OAuth
 */
public enum AccessMode {
    LOCAL, GOOGLE
}
